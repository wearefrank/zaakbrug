<Module xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../FrankConfig.xsd">
    <Adapter name="SoapEndpointProxy_VrijeBerichten"
        active="${SoapEndpointProxy_VrijeBerichten.Active}"
        description="This adapter will send the message to proxy">

        <Receiver name="SoapEndpointProxy_VrijeBerichten">
            <WebServiceListener name="SoapEndpointProxy_VrijeBerichten" address="${zaakbrug.soap.proxy.vrije-berichten.endpoint}" soap="false"/>
        </Receiver>

        <Receiver name="SoapEndpointProxy_VrijeBerichten_Java">
            <JavaListener name="SoapEndpointProxy_VrijeBerichten"/>
        </Receiver>

        <Pipeline>
            <Exits>
                <Exit name="EXIT" state="SUCCESS"/>
                <Exit name="EXCEPTION" state="ERROR"/>
            </Exits>

            <SenderPipe
                    name="SendToProxy">
                <WebServiceSender
                        name="SendToProxySender"
                        soap="false"
                        soapActionParam="SOAPAction"
                        url="${zaakbrug.soap.proxy.vrije-berichten.proxy.url}"
                        timeout="${zaakbrug.soap.proxy.vrije-berichten.proxy.timeout}"
                        verifyHostname="${zaakbrug.soap.proxy.vrije-berichten.proxy.verifyHostname}"
                        allowSelfSignedCertificates="${zaakbrug.soap.proxy.vrije-berichten.proxy.allowSelfSignedCertificates}"
                        disableCookies="${zaakbrug.soap.proxy.vrije-berichten.proxy.disableCookies}"
                        ignoreCertificateExpiredException="${zaakbrug.soap.proxy.vrije-berichten.proxy.ignoreCertificateExpiredException}"
                        truststore="${zaakbrug.soap.proxy.vrije-berichten.proxy.truststore.uri}"
                        truststoreAuthAlias="${zaakbrug.soap.proxy.vrije-berichten.proxy.truststore.authAlias}"
                        truststoreType="${zaakbrug.soap.proxy.vrije-berichten.proxy.truststore.type}"
                        keystore="${zaakbrug.soap.proxy.vrije-berichten.proxy.keystore.uri}"
                        keystoreAlias="${zaakbrug.soap.proxy.vrije-berichten.proxy.keystore.alias}"
                        keystoreAuthAlias="${zaakbrug.soap.proxy.vrije-berichten.proxy.keystore.authAlias}"
                        keystoreType="${zaakbrug.soap.proxy.vrije-berichten.proxy.keystore.type}">
                    <Param name="SOAPAction" sessionKey="SOAPAction" />
                </WebServiceSender>
                <Forward name="success" path="EXIT" />
                <Forward name="exception" path="EXCEPTION" />
            </SenderPipe>
        </Pipeline>
    </Adapter>
</Module>