<Module xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../FrankConfig.xsd">
    <Adapter name="SoapEndpointReplicator_VrijeBerichten"
        active="${SoapEndpointReplicator_VrijeBerichten.Active}"
        description="This adapter will first send the message to the endpoint router, and (optionally only if the response is successful) it also sends the message the proxy">

        <Receiver name="SoapEndpointReplicator_VrijeBerichten">
            <WebServiceListener name="SoapEndpointReplicator_VrijeBerichten" address="${zaakbrug.soap.replicator.vrije-berichten.endpoint}" soap="false"/>
        </Receiver>

        <Pipeline>
            <Exits>
                <Exit name="EXIT" state="SUCCESS"/>
                <Exit name="EXCEPTION" state="ERROR"/>
            </Exits>

            <SenderPipe
                    name="SendToRouter"
                    preserveInput="true"
                    storeResultInSessionKey="EndpointRouterResponse">
                <FrankSender
                        name="SendToRouterSender"
                        target="SoapEndpointRouter_VrijeBerichten"
                        returnedSessionKeys="SOAPAction, exitState"/>
                <Forward name="success" path="SendToProxy" />
                <Forward name="exception" path="CheckIfShouldSendToProxyAnyways" />
            </SenderPipe>

            <IfPipe name="CheckIfShouldSendToProxyAnyways" getInputFromFixedValue="${zaakbrug.soap.replicator.vrije-berichten.shadowMode}">
                <Forward name="then" path="SendToProxy"/>
                <Forward name="else" path="EXCEPTION"/>
            </IfPipe>

            <SenderPipe name="SendToProxy">
                <FrankSender name="SendToProxySender" target="SoapEndpointProxy_VrijeBerichten" synchronous="false">
                    <Param name="SOAPAction" sessionKey="SOAPAction"/>
                </FrankSender>
            </SenderPipe>

            <EchoPipe name="EchoEndpointRouterResponse" getInputFromSessionKey="EndpointRouterResponse"/>

            <IfPipe name="IfEndpointRouterSuccess" getInputFromSessionKey="exitState" expressionValue="SUCCESS">
                <Forward name="then" path="EXIT"/>
                <Forward name="else" path="EXCEPTION"/>
            </IfPipe>
        </Pipeline>
    </Adapter>
</Module>